import React from "react";

function Article() { // declare function 'Article'
  return ( // return value of JSX
    <div>Dear Reader: Bjarne Stroustrup has the perfect lecture oration.</div>
  ); // "when you want to put this component on the DOM, here's what it should be"
}

export default Article;